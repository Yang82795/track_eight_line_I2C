# 重走古丝路 · 自动循迹避障小车 (V4.0 实战优化版)
## 比赛调试与快速参数修改指南

---

### 一、 为什么重构为 V4.0 版本？

| 模块 | 原版（original）存在的痛点 | 3.0 版本（过度设计）带来的隐患 | V4.0（本版本）的解决方案 |
| :--- | :--- | :--- | :--- |
| **8路红外通信** | 基础查表，仅覆盖 14 种状态，有遗漏死角 | 擅自删除 10ms 延时，改 Repeated-Start，通信极易丢帧 | **保留标准 10ms 硬件延时**；全状态连续加权，零死角平滑 |
| **急弯与发夹弯** | 全程固定速度 90，遇到赛段4发夹弯易冲出 | 虚构软里程计，开环死算，打滑时计算严重失真 | **自适应弯道降速**：直道 100 全速，急弯自动降到 70 强抓地 |
| **超声波与避障** | 原版只有简单延时演示，未实现闭环回线 | 4点舵机扫描拖慢半秒，且偏出后全白状态盲开冲出 | **保留标准 HCSR04 库**，快速左右择优，**红外见线即止闭环回线** |
| **终点 20 分停车** | 滑行断电，车尾极易冲过终点线丢 20 分 | “先刹后判”在赛道中间乱急刹慢爬，破坏比赛节奏 | **起步时间锁 + 连续双帧全宽横线 + 90ms 电子反接制动定死** |
| **可维护性** | 简单但功能未完善 | 7个文件上千行，状态机与宏定义盘根错节，现场难调 | **结构清晰**，参数集中在各文件顶部，改数字即生效 |

---

### 二、 硬件引脚接线确认表 (100% 对齐原版)

| 硬件功能 | 引脚号 | 模式 / 协议 | 说明 |
| :--- | :--- | :--- | :--- |
| 左电机 PWM | **D3** | PWM 输出 | 左轮速度控制 |
| 左电机 IN1 / IN2 | **D4 / D5** | 数字输出 | 控制左电机正反转 |
| 右电机 PWM | **D11** | PWM 输出 | 右轮速度控制 |
| 右电机 IN1 / IN2 | **D9 / D10** | 数字输出 | 控制右电机正反转 |
| 超声波 Trig | **D8** | 数字输出 | 触发测距 |
| 超声波 Echo | **D7** | 数字输入 | 接收回波 (由 `<HCSR04.h>` 驱动) |
| 避障云台舵机 | **D6** | PWM 舵机输出 | 中位 95°，左探 65°，右探 125° |
| 起跑按键 KEY | **D2** | 上拉输入 | `INPUT_PULLUP`，按下为 LOW |
| 8路红外循迹模块 | **A4(SDA) / A5(SCL)** | I2C 总线 | 设备地址 `0x12`，数据寄存器 `0x30` |

---

### 三、 现场实战调参速查（需要微调时改哪个数字）

所有核心可调参数均集中在对应文件的顶部 `#define` 中：

#### 1. 速度与 PID 参数 (`line_pid.hpp`)
* `Speed_Line` (默认 `100`)：直道巡线基准速度。现场如果路面抓地好，可尝试拉到 `105~115` 冲高时间分；若小车发飘，降到 `90`。
* `Speed_Curve` (默认 `85`)：普通弯道速度。
* `Speed_Sharp` (默认 `70`)：赛段 4 发夹弯与大急弯的自动限速速度。保证小车大角度打方向时不因惯性甩飞。
* `KPx` (默认 `18.5f`)：转向响应比例。如果小车切弯不够灵敏、压不住线，**微调加大**（每次 +2.0）；如果直线小车剧烈“画龙/摆头”，**微调减小**（每次 -2.0）。
* `KDx` (默认 `8.0f`)：微分阻尼。用于消除小车摆头晃荡。摆动大时适当增加。

#### 2. 避障动作参数 (`avoid_obstacle.hpp`)
* `AVOID_TRIGGER_CM` (默认 `25`)：前方检测到障碍物多少厘米开始绕行。如果车速快来不及刹车，改大到 `28` 或 `30`。
* `TIME_TURN_OUT_MS` (默认 `360`)：第一阶段向开阔侧打方向偏出黑线的时间（毫秒）。
  * 若小车转角不够、车头会蹭到障碍物侧边：**适当改大到 `380~420`**；
  * 若小车偏出过大、险些跑出广告布边界：**适当改小到 `300~340`**。
* `TIME_PASS_MS` (默认 `520`)：第二阶段平行超越障碍物纵向长度的时间。
* 闭环寻线（第三阶段）：小车在切回过程中不断读取 I2C 红外，只要中间探头（x3~x6）一碰到黑线立即停止切回，无需盲猜时间！

#### 3. 终点防滑行与起跑 (`track_eight_line_I2C.ino`)
* `AUTO_START` (默认 `0`)：
  * 设为 `0`：对齐起点线后，按下小车板载 KEY 键起跑（推荐比赛使用，便于人工精确瞄准方向）；
  * 设为 `1`：上电 2 秒后自动发车。
* `FINISH_SHIELD_MS` (默认 `14000` = 14秒)：起跑保护时间。赛道全长 6.2 米，小车正常跑完需要 20~30 秒。在前 14 秒内，系统屏蔽终点横线判定，防止车头压在 500mm 起点横线时误识别提前停下。
* `BRAKE_MS` (`motor_car.cpp` 顶部，默认 `90`)：终点主动刹车反向通电时间（毫秒）。如果小车由于轮子较重停下后还有微小滑行，可改大到 `110~130`，彻底定死在终点线内拿下 20 分！

#### 4. 直行机械跑偏校正 (`motor_car.hpp`)
* `MOTOR_LEFT_SCALE` (默认 `1.00f`)
* `MOTOR_RIGHT_SCALE` (默认 `1.00f`)
  * 若给相同速度小车天生向右跑偏：说明右轮转速慢或左轮快，将 `MOTOR_LEFT_SCALE` 改为 `0.97f` 或 `MOTOR_RIGHT_SCALE` 改为 `1.03f`。

---

### 四、 现场 15 分钟调试黄金流程

1. **第一步（静态测电与探头）**：
   小车连接电脑串口（波特率 115200），把小车放在赛道上推移。观察串口打印的 `[x1..x8]`：压在黑线上对应的位应为 `0`，白底应为 `1`；且压在起点横线上时 `Blk` 应显示为 6~8。
2. **第二步（测避障与舵机方向）**：
   在小车正前方 20cm 处放一个纸盒，小车应刹停，舵机应先向左扭头、再向右扭头。
3. **第三步（单圈全赛段试跑）**：
   放在起点线对齐，按下按键出发。观察赛段 4 发夹弯是否顺畅通过，重点看终点线前是否稳稳刹住、车尾不越线。
4. **第四步（正式比赛封箱）**：
   将 `track_eight_line_I2C.ino` 中的 `#define DEBUG_SERIAL 1` 改为 `0`，拔掉 USB 下载线，避免串口输出占用单片机时间，享受满分完赛！
