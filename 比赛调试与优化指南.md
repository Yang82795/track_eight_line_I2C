# 重走古丝路循迹避障小车：比赛调试与优化指南

> 本文依据仓库中的《附件1 自动循迹避障小车比赛规则与评分标准》《附件2_小车制作评分标准》《附件3_现场演讲评分标准》、赛道地图及当前代码整理。代码行号以当前版本为准；以后行号变化时，以文中给出的**常量名/函数名**搜索定位。

## 1. 先看比赛得分：优化顺序不要反

单轮比赛分数为：

`（赛段分 40 + 避障分 40 + 时间分 40 + 停止分 20）× 合规系数`

结合规则，建议优先级是：

1. **合规并能全程到终点**：未到终点没有时间分；逆行、全轮出界会直接结束本轮。
2. **4 次无接触避障并回线**：每个 10 分，车的任何部件接触障碍都不得分；障碍从车底穿过也不得分。
3. **终点及时停车**：20 分，停车后车尾投影不能越过终点线。
4. **最后才提速**：时间分是 `40 × 最快完赛时间 / 本车时间`。不稳定地提速很容易同时损失赛段、避障、停止和时间分。

当前代码的总体方向正确：8 路质心循迹、弯道降速、丢线搜索、非阻塞避障、左右测距选路和终点横线停车，都比固定动作可靠。但所有运动时间和传感器阈值都仍是**初始值**，不能不经实车标定直接参赛。

---

## 2. 目前还可优化或必须验证的地方

### P0：上场前必须确认

#### 2.1 传感器极性、左右位序和电机方向

当前程序假设：

- `x1` 是车体最左探头，`x8` 是最右探头；
- 黑线输出 `0`，白底输出 `1`；
- `Set_speed(正数, 0)` 前进；
- 正的转向修正使车辆向右。

任一假设不符，PID 再怎么调也无法稳定。对应位置：

- 位序/极性：`track_eight_line_I2C.ino` 的 `readLineSensor()`，当前约第 111～136 行；
- 传感器权重：`line_pid.cpp` 的 `kSensorPosition`，当前第 16 行；
- 电机方向：`motor_car.cpp` 的 `setMotorSpeed()`，当前约第 39～57 行。

如果黑线实际输出 `1`，将 `readLineSensor()` 中：

```cpp
const uint8_t data = Wire.read();
```

改成：

```cpp
const uint8_t data = ~Wire.read();
```

不要同时反转位序和极性；一次只修正一个问题并复测。

#### 2.2 超声波是否能看到“偏在线一侧”的全部四种障碍物

规则不是说障碍物一定正对超声波，而是会随机放在循迹线任意一侧。障碍最小只有 `60 mm × 60 mm`，正前方单个超声波可能漏掉边缘的小障碍；赛段完成标识约 `120 mm × 50 mm × 120 mm`，也可能被误认为障碍。

必须用与实物尺寸相近的纸盒，在黑线左侧、右侧、不同弯道分别测试。修改位置：

- 舵机朝向：`SERVO_CENTER_ANGLE / SERVO_LEFT_ANGLE / SERVO_RIGHT_ANGLE`（主程序约第 22～24 行）；
- 触发距离：`OBSTACLE_DETECT_CM`（约第 27 行）；
- 紧急距离：`OBSTACLE_EMERGENCY_CM`（约第 28 行）；
- 连续确认逻辑：`watchForObstacle()`（约第 205～219 行）。

**判断标准**：最小障碍左右两种摆法各测试至少 10 次，不能漏检；经过赛段标识时不能频繁误绕。若两者无法兼顾，优先调整超声波安装高度、前伸位置和中位角，而不是一味增大检测距离。

#### 2.3 终点停车位置

程序用“至少 7 路同时黑色并持续 45 ms”识别终点，并在启动 8 s 后才使能。风险有两个：

- `FINISH_ARM_AFTER_MS = 8000` 必须小于本车最快到终点时间，否则高速完赛可能不停；
- `StopCar()` 当前是 PWM 置零和方向脚全低，具体是滑行还是制动取决于电机驱动板，车辆可能靠惯性越过终点，丢 20 分。

修改位置：

- 横线判定：`line_pid.cpp` 的 `lineCrossing = (blackCount >= 7)`，当前第 73 行；
- 终点参数：主程序 `FINISH_ARM_AFTER_MS / FINISH_CONFIRM_MS`，约第 46～47 行；
- 停车行为：`motor_car.cpp` 的 `StopCar()`，约第 29～37 行；
- 终点状态：主程序 `checkForFinish()`，约第 221～235 行。

先不要直接把 `StopCar()` 改成高电平制动。先查清驱动板型号和真值表；错误的主动制动接法可能造成桥臂直通。用最低、正常、满电三种电量各做 5 次终点测试，以“车头过线且停车后车尾未过线”为合格。

### P1：强烈建议优化

#### 2.4 避障动作目前完全按时间开环，电池电压变化会改变轨迹

`AVOID_TURN_MS / OUTWARD_MS / PASS_MS` 都受轮胎摩擦、电池电量、左右电机差异影响。比赛的四个障碍沿线尺寸分别约 150、60、120、110 mm；现参数统一按最大障碍留余量，稳定但会损失时间。

建议先保留统一保守参数拿满避障分。只有全程成功率达到 90% 以上后，才考虑根据测距或障碍计数缩短动作；不要为了几秒时间分冒险损失一个 10 分避障。

参数位置：主程序约第 33～42 行，重点是：

- `AVOID_TURN_PWM / AVOID_TURN_MS`：原地转角；
- `AVOID_DRIVE_PWM / AVOID_OUTWARD_MS`：横向净空；
- `AVOID_PASS_MS`：车尾越过障碍所需长度；
- `AVOID_INWARD_MIN_MS / AVOID_INWARD_TIMEOUT_MS`：回线窗口；
- `OBSTACLE_RETRIGGER_LOCKOUT_MS`：防止重复识别同一障碍。

#### 2.5 左右扫描只有单次测距，建议先验证离群值

现在舵机稳定后左右各读一次，单次超声波反射异常会选错方向。函数位置：`DRIVE_SCAN_LEFT / DRIVE_SCAN_RIGHT`，约第 249～271 行。

若日志中同一位置的读数跳动超过约 5～10 cm，可后续把每侧改为 3 次采样取中值。注意不能用长 `delay()`，可增加非阻塞子状态或短间隔计数。实测稳定时不要为了“算法更复杂”而改动。

#### 2.6 超声波无回波时的阻塞时间要测

`readDistanceCm()` 调用库的 `ultrasonic.dist()`。不同 HCSR04 库版本对 `pulseIn` 超时处理不同；无回波若阻塞几十毫秒，会降低 PID 更新频率，在急弯处引发摆动。位置：主程序约第 100～105 行。

打开串口记录循环间隔，在空旷无回波与正前方有物体两种情况下比较。若控制周期出现明显的几十毫秒尖峰，再改用带明确超时的测距实现；没有数据前不要盲改库接口。

#### 2.7 丢线搜索不能掩盖机械问题

当前失线后会原地朝最后看到线的一侧搜索（`line_pid.cpp` 的 `Car_line_track()`，约第 122～143 行）。这适合作为兜底，但如果普通弯道频繁进入失线搜索，会耗时并存在逆转风险。

应优先降低直线速度、提高弯道修正或调整传感器前伸量；不要只提高 `LOST_LINE_TURN_PWM`。

### P2：稳定后再做

- 增加每次避障计数与各状态耗时日志，确认恰好执行 4 次避障；正式比赛必须把 `SERIAL_DEBUG` 改回 `0`。
- 在不影响可靠性的前提下提高 `LINE_SPEED_STRAIGHT`，弯道速度保持保守。
- 若场地实测终点横线只能覆盖 6 路，可将横线阈值由 7 改为 6，但必须证明普通急弯/大黑色图案不会误触发。
- 可做低电压补偿，但当前没有电池电压采样硬件，不建议只靠运行时间猜测电量。

---

## 3. 实车调试步骤（必须按顺序）

### 步骤 0：机械与规则检查

- 实测整车外廓不超过 `250 × 200 × 200 mm`；建议留装配误差，不要卡上限。
- 车架必须自制，电池只能一个且装在车内，所有部件固定可靠。
- 超声波、舵机、线阵不能伸出后晃动；线阵尽量低且加车载遮光罩。
- 重心低、左右接近对称；线阵和超声波支架不能在避障时先碰到模型。
- 保存 CAD/SolidWorks 源文件、加工照片/视频和装配过程证据。

记录：整车长/宽/高、轮距、轴距、传感器离地高度、传感器到车头及车尾的距离。这些数据决定避障和终点参数。

### 步骤 1：离地架空测试电机

1. 将驱动轮架空。
2. 暂时单独调用 `Set_speed(80, 0)`，确认两轮都使车向前。
3. 调用 `Set_speed(0, 80)`，确认车辆趋势为向右转。
4. 若方向错误，先检查接线，再按 2.1 的代码位置修改。

**通过条件**：前进、后退、左右转含义与代码注释一致，无电机堵转和驱动板异常发热。

### 步骤 2：确认 8 路数据

1. 将主程序 `SERIAL_DEBUG` 从 `0` 改为 `1`（约第 50 行）。
2. 白底时读取 `black=0`；用 20 mm 黑胶带依次压最左、中间、最右探头。
3. 确认黑线右移时误差方向使车右转。
4. 用 500 mm 长横线测试，确认 `black` 接近 7～8。

当前日志没有直接输出 8 位原始值；若无法判断顺序，可在 `readLineSensor()` 后临时打印 `data` 的二进制值。确认后删除临时代码，避免串口拖慢循环。

### 步骤 3：校正机械直行

1. 暂不放黑线和障碍，以相同 PWM 前进 1～2 m。
2. 若总向左偏，略降左轮比例或略升右轮比例；向右偏反之。
3. 修改 `motor_car.cpp` 第 10～11 行附近的 `MOTOR_LEFT_SCALE / MOTOR_RIGHT_SCALE`，每次只改 `0.01～0.02`。

**通过条件**：1 m 内偏移足够小，且不同电池电量下方向一致。机械偏差不要靠大积分修复。

### 步骤 4：只调循迹，不放障碍

修改 `line_pid.cpp` 顶部：

1. 先将 `LINE_KI` 暂设为 `0`，`LINE_SPEED_STRAIGHT` 保守设为约 `90～110`。
2. 调 `LINE_KP`：转弯迟钝就每次加 1～2；左右高频摆动就每次减 1～2。
3. 调 `LINE_KD`：弯后过冲时小幅增加；若电机抖动、噪声放大则减小。
4. 最后仅在存在稳定静差时加入很小 `LINE_KI`。
5. 分别测试地图左侧大回环、中部大弯、右侧连续 S 弯。
6. 稳定后每次提高直线 PWM 5～10，直到出现首次不稳定，再退回至少一级。

**通过条件**：连续完整循迹 10 圈/次无脱线；轻推车身后能自行恢复；最低和满电状态都通过。

### 步骤 5：标定舵机和测距

1. 保持车轮离地，依次让舵机写入 CENTER、LEFT、RIGHT。
2. 从车体上方确认方向，反了就交换 `SERVO_LEFT_ANGLE` 和 `SERVO_RIGHT_ANGLE`。
3. 在 10、20、30、40 cm 放置障碍，各测 10 次并记录平均值和最大误差。
4. 用 60 mm 宽最小模拟障碍在黑线左右边缘测试能否被发现。
5. 检查舵机转动是否造成 MCU 复位；复位通常是供电压降或地线问题，不是软件问题。

### 步骤 6：分解标定避障几何动作

使用最大模拟障碍 `150 × 100 × 150 mm`，并把车身所有突出件计入安全距离。

1. **转角**：调 `AVOID_TURN_PWM / AVOID_TURN_MS`，使每次原地转约 45°，左右方向都各测 5 次。
2. **离线距离**：调 `AVOID_OUTWARD_MS`，让车体最近点与障碍保持明确余量；不要只看轮子。
3. **平行越障**：调 `AVOID_PASS_MS`，确保车尾完全超过障碍后才回转。
4. **回线**：调 `AVOID_INWARD_MIN_MS`，使线阵重新压线时立即进入对正，不横穿过头。
5. **失败兜底**：故意让回线稍偏，检查 `AVOID_INWARD_TIMEOUT_MS` 后能否由失线搜索找回。

一次只改一个参数，每组做左绕、右绕各 5 次。通过后再测试 60、120、110 mm 三种尺寸。

### 步骤 7：随机组合压力测试

让另一名队员随机摆放 4 个障碍，调车人员不要提前知道方向。至少完成 10 次全程测试，表格记录：

| 次数 | 完赛时间 | 4 段通过 | 避障成功数 | 是否碰撞 | 是否脱线 | 终点停车合格 | 电池电压 | 失败状态/参数 |
| --- | ---: | ---: | ---: | --- | --- | --- | ---: | --- |
| 1 | | | | | | | | |

**建议正式版门槛**：10 次中至少 9 次完赛，且无碰撞成功率明显高于提速版。若做不到，回退到上一个稳定参数版本。

### 步骤 8：终点专项测试

1. 按真实起跑姿态让车头投影对齐起始线，确认起点横线不会触发停车。
2. 测出最快到终点时间，将 `FINISH_ARM_AFTER_MS` 设得显著小于该时间，但又晚于起点和任何可能的大面积黑色干扰。
3. 在最低、常用、满电电量下各跑 5 次，测停车后车尾到终点线的余量。
4. 若冲过线：先降低横线状态下速度、缩短 `FINISH_CONFIRM_MS`；仍不足再根据驱动真值表评估主动制动。
5. 若线前误停：检查黑色图案是否覆盖 7 路，再提高阈值/确认时间，而不是关闭终点停车。

### 步骤 9：生成正式比赛固件

- `SERIAL_DEBUG` 改回 `0`；
- 删除所有临时 `delay()`、电机单测代码和原始数据刷屏；
- `FINISH_STOP_ENABLED` 保持 `true`；
- 启用一种无需赛中人工干预的启动方式；
- 给最终参数拍照/备份，标明日期、电池型号、轮胎状态；
- 用最终固件再完整跑 3 次，不要在比赛前临时增加未经验证的功能。

---

## 4. 参数修改速查表

| 现象 | 先改哪里 | 建议方向 |
| --- | --- | --- |
| 直行始终偏一边 | `motor_car.cpp`：`MOTOR_*_SCALE` | 偏哪侧通常就略降该侧速度；每次 0.01～0.02 |
| S 弯左右摆动 | `line_pid.cpp`：`LINE_KP/KD` | 先降 KP，再小幅升 KD |
| 转弯迟钝、黑线到边缘才转 | `line_pid.cpp`：`LINE_KP` | 小幅提高；必要时降低直线/弯道速度 |
| 急弯失线 | `LINE_SPEED_TURN`、传感器前伸量 | 先降弯速，检查机械安装，再调 PID |
| 丢线搜索方向反了 | `readLineSensor()` 位序或 `kSensorPosition` | 修正左右位序，不要靠 PID 符号硬补 |
| 看不到最小障碍 | `SERVO_CENTER_ANGLE`、安装位置、`OBSTACLE_DETECT_CM` | 先调安装和朝向，再谨慎增距离 |
| 误把标识当障碍 | `OBSTACLE_DETECT_CM`、`watchForObstacle()` | 缩短距离/增加合理确认，但不能漏掉 60 mm 障碍 |
| 转出角不足/过大 | `AVOID_TURN_MS` | 保持 PWM 后小步改时间 |
| 车侧面擦障碍 | `AVOID_OUTWARD_MS` | 增大，且按车身最突出部位测量 |
| 车尾碰障碍 | `AVOID_PASS_MS` | 增大 |
| 回线横穿过头 | `AVOID_INWARD_MIN_MS` / `AVOID_OUTWARD_MS` | 缩短回线最小时间或横移距离 |
| 回线后再次绕同一物体 | `OBSTACLE_RETRIGGER_LOCKOUT_MS` | 适当增加，但不能大到漏掉下一障碍 |
| 到终点不停 | `FINISH_ARM_AFTER_MS`、黑线阈值、极性 | 先看日志确认是否识别横线 |
| 终点停车越线 | `FINISH_CONFIRM_MS`、横线速度、`StopCar()` | 先缩确认时间/降横线速度，再评估安全制动 |

---

## 5. 15 分钟比赛现场策略

规则给每队总计 15 分钟、最多 3 次正式比赛，每次上限 5 分钟，取最好成绩。建议：

1. 进场先用约 2～3 分钟检查光照、广告布摩擦、超声波和起终点识别；只改一个最关键参数。
2. 第一轮使用“稳定参数”，目标是拿到完整赛段和避障分，保留有效成绩。
3. 若第一轮失败，根据明确故障只改对应参数；不要同时改 PID、速度和避障时间。
4. 已有稳定完赛成绩后，第三轮才考虑使用略快参数冲时间分。
5. 每次交卡前确认电池、轮胎、舵机中位、开关、线缆；交卡后不得再调试。

---

## 6. 车架和演讲也直接计分

### 车架制作分

附件 2 强调的不只是“能跑”：

- 稳固固定电机、轮子、传感器、主板和电池；
- 展示低重心、灵活转向、轻量化、模块化和针对避障的小尺寸设计；
- 主要结构尽量使用 3D 打印、激光切割或数控加工；
- 保留设计源文件、尺寸图、加工照片/视频、迭代前后对比。

### 5 分钟演讲

按附件 3 的四个 25 分项组织 PPT：

1. **项目内容**：硬件框图、传感器选型、控制状态机、技术路线；
2. **创新与难点**：8 路质心 PID、丢线恢复、左右扫描自选绕行、非阻塞状态机、终点识别，并用测试数据说明解决效果；
3. **成本**：逐项 BOM、总成本、200 元报销范围及性价比；
4. **表达与答问**：控制在 5 分钟，准备“为什么不用固定绕行”“如何防误判标识”“电量变化如何处理”“如何保证不碰障碍”等问题。

最有说服力的材料不是代码截图，而是**成功率、完赛时间、不同电量测试、左右随机避障次数、改进前后对比**。

---

## 7. 最终检查清单

- [ ] 外廓小于等于 `250 × 200 × 200 mm`
- [ ] 自制主车架及加工证据齐全
- [ ] 单一车载电源，电池固定并有安全保护
- [ ] 不依赖遥控、电脑或赛中人工干预
- [ ] 8 路极性、左右顺序、电机方向全部确认
- [ ] 左右舵机方向与变量名称一致
- [ ] 最小障碍左右随机放置不漏检
- [ ] 最大障碍左右绕行均无任何部件接触
- [ ] 连续 10 次全程成功率达到正式版门槛
- [ ] 起点不误停，终点停车位置满足 20 分条件
- [ ] `SERIAL_DEBUG = 0`
- [ ] 最终固件和最终参数已备份
- [ ] 准备同型号备用电池、维修工具和比赛记录表
